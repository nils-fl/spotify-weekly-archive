[project]
name = "spotify-helpers"
version = "0.1.0"
description = "Weekly archive of Spotify Discover Weekly into a permanent playlist"
requires-python = ">=3.10,<3.11"
dependencies = [
    "librespot",
    "requests",
]

[tool.uv]
package = false

[tool.uv.sources]
librespot = { git = "https://github.com/kokarare1212/librespot-python.git" }
